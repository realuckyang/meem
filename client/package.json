{
  "name": "meem-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:bridge": "mkdir -p tauri/binaries && bun build ../server/src/index.js --compile --outfile tauri/binaries/meem-bridge-aarch64-apple-darwin",
    "tauri": "tauri",
    "tauri:dev": "npm run build:bridge && tauri dev --config tauri/tauri.conf.json",
    "tauri:build": "npm run build:bridge && tauri build --config tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
