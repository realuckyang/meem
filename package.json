{
  "name": "meem",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "server": "npm --prefix server start",
    "worker:dev": "npm --prefix worker run dev",
    "worker:deploy": "npm --prefix worker run deploy",
    "gui:dev": "npm --prefix worker/gui run dev",
    "gui:build": "npm --prefix worker/gui run build",
    "browser:mcp": "npm --prefix browser-use run mcp",
    "browser:check": "npm --prefix browser-use run check"
  }
}
