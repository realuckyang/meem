{
  "name": "meem-new",
  "version": "0.2.0",
  "private": true,
  "description": "Meem (local-only edition): server + gui, 无 Cloudflare Worker 依赖",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix gui",
    "build": "npm run build --prefix gui",
    "start": "npm start --prefix server",
    "serve": "npm run build && npm start"
  }
}
